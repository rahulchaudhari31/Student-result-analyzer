# Base URL of your FastAPI backend.
# Local dev uses the Vite proxy (http://localhost:5000) so this can stay empty locally.
# Production builds automatically fall back to the deployed Render backend
# (https://new-result-analysis-system.onrender.com) when this is unset.
VITE_API_URL=
